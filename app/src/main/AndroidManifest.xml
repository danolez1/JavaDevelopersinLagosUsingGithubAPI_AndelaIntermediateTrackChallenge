<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.supreme.javadevelopersinlagosusinggithubapi_andelaintermediatetrackchallenge">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/andela_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme_">
        <activity android:name=".Activities.IntroSliderActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.GitHub_Users_LagosActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".Activities.GetStartedActivity" />

        <activity android:name=".Activities.AboutAppActivity" />
        <activity
            android:name=".Activities.UserDetailsActivity"
            android:label="@string/title_activity_user_details"
            android:theme="@style/AppTheme"></activity>
        <!--<activity android:name=".Activities.AboutAppActivity$AboutAppActivity" />!-->
    </application>

</manifest>